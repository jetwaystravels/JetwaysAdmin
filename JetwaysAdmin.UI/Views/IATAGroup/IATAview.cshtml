<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>IATA Groups – Jetways</title>
    <link rel="stylesheet" href="https://rsms.me/inter/inter.css">
    <style>
        :root {
            --bg: #f7f8fb;
            --panel: #ffffff;
            --text: #0f172a;
            --muted: #6b7280;
            --line: #e5e7eb;
            --primary: #2563eb;
            --primary-700: #1d4ed8
        }

        * {
            box-sizing: border-box
        }

        body {
            margin: 0;
            background: var(--bg);
            color: var(--text);
            font: 14px/1.4 Inter,system-ui,-apple-system,Segoe UI,Roboto,Ubuntu,"Helvetica Neue",Arial,"Noto Sans",sans-serif
        }

        .page {
            max-width: 1200px;
            margin: 28px auto;
            padding: 0 20px
        }

        .breadcrumb {
            color: var(--muted);
            font-size: 13px;
            margin-bottom: 10px
        }

        .back {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 28px;
            height: 28px;
            border-radius: 7px;
            border: 1px solid var(--line);
            background: #fff;
            margin-right: 8px;
            cursor: pointer;
            text-decoration: none;
            color: inherit
        }

        .panel {
            background: var(--panel);
            border: 1px solid var(--line);
            border-radius: 12px;
            box-shadow: 0 2px 8px rgba(15,23,42,.04)
        }

        .toolbar {
            display: flex;
            gap: 12px;
            justify-content: space-between;
            align-items: center;
            padding: 12px 14px;
            border-bottom: 1px solid var(--line)
        }

        .searchbox {
            position: relative;
            max-width: 360px;
            flex: 1
        }

            .searchbox input {
                width: 100%;
                height: 36px;
                padding: 0 12px;
                border-radius: 9px;
                border: 1px solid var(--line);
                background: #fafafa;
                outline: none
            }

        .add-btn {
            display: inline-flex;
            gap: 8px;
            align-items: center;
            background: var(--primary);
            color: #fff;
            border: none;
            border-radius: 9px;
            padding: 8px 12px;
            text-decoration: none
        }

        table {
            width: 100%;
            border-collapse: separate;
            border-spacing: 0
        }

        thead th {
            font-weight: 600;
            font-size: 12px;
            color: var(--muted);
            text-align: left;
            padding: 10px 14px;
            background: #fafafa;
            border-bottom: 1px solid var(--line)
        }

        tbody td {
            padding: 14px;
            border-bottom: 1px solid var(--line)
        }

        .switch {
            --w: 38px;
            --h: 22px;
            --p: 3px;
            position: relative;
            display: inline-block;
            width: var(--w);
            height: var(--h)
        }

            .switch input {
                opacity: 0;
                width: 0;
                height: 0
            }

        .slider {
            position: absolute;
            cursor: pointer;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: #d1d5db;
            border-radius: 9999px;
            transition: all .25s
        }

            .slider:before {
                content: "";
                position: absolute;
                height: calc(var(--h) - var(--p)*2);
                width: calc(var(--h) - var(--p)*2);
                left: var(--p);
                top: var(--p);
                background: #fff;
                border-radius: 50%;
                transition: all .25s;
                box-shadow: 0 1px 3px rgba(0,0,0,.2)
            }

        .switch input:checked + .slider {
            background: #60a5fa
        }

            .switch input:checked + .slider:before {
                transform: translateX(calc(var(--w) - var(--h)))
            }

        .footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 14px;
            color: var(--muted);
            font-size: 12px
        }

        .chip {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            padding: 4px 8px;
            border: 1px solid var(--line);
            border-radius: 8px;
            background: #fff
        }

        .select {
            border: none;
            background: transparent;
            outline: none
        }
    </style>
</head>
<body>
    <div class="page">
        <div class="breadcrumb">Settings / IATA Groups</div>
        <h2><a class="back" href="#" onclick="history.back();return false;" title="Back">←</a> IATA Groups</h2>

        <div class="panel">
            <div class="toolbar">
                <div class="searchbox">
                    <input id="search" placeholder="Search" />
                </div>
                <!-- Link to the form page -->
                <a asp-controller="IATAGroup"
                   asp-action="AddIATAGroup"
                   class="add-btn">+ Add New</a>
            </div>

            <table id="groupsTable" aria-describedby="table-caption">
                <caption id="table-caption" style="position:absolute;left:-9999px">Manage IATA Group locations</caption>
                <thead>
                    <tr>
                        <th>Group Name</th>
                        <th>Group Code</th>
                        <th>City,State,Country</th>
                        <th>Address</th>
                        <th style="width:110px">Status</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>Jetways Delhi</td>
                        <td>DEL</td>
                        <td>India, Delhi, New Delhi</td>
                        <td>CONNAUGHT PLACE , NEW DELHI , UGF …</td>
                        <td>
                            <label class="switch">
                                <input type="checkbox" checked aria-label="Jetways Delhi active" />
                                <span class="slider"></span>
                            </label>
                        </td>
                    </tr>
                    <tr>
                        <td>Jetways Gurgaon</td>
                        <td>—</td>
                        <td>India, Haryana, Gurgaon</td>
                        <td>—</td>
                        <td>
                            <label class="switch">
                                <input type="checkbox" checked aria-label="Jetways Gurgaon active" />
                                <span class="slider"></span>
                            </label>
                        </td>
                    </tr>
                    <tr>
                        <td>Jetways Pune</td>
                        <td>PNQ</td>
                        <td>India, Delhi, New Delhi</td>
                        <td>B. K. ROAD, CONNAUGHT PLACE, UGF …</td>
                        <td>
                            <label class="switch">
                                <input type="checkbox" checked aria-label="Jetways Pune active" />
                                <span class="slider"></span>
                            </label>
                        </td>
                    </tr>
                    <tr>
                        <td>Jetways Mumbai</td>
                        <td>—</td>
                        <td>India, Maharashtra, Mumbai</td>
                        <td>—</td>
                        <td>
                            <label class="switch">
                                <input type="checkbox" checked aria-label="Jetways Mumbai active" />
                                <span class="slider"></span>
                            </label>
                        </td>
                    </tr>
                </tbody>
            </table>

            <div class="footer">
                <div>1 – 4 of 4</div>
                <div>
                    <span class="chip">
                        Items per page:
                        <select class="select">
                            <option selected>25</option>
                            <option>50</option>
                            <option>100</option>
                        </select>
                    </span>
                </div>
            </div>
        </div>
    </div>

    <script>
        const search = document.getElementById('search');
        const rows = Array.from(document.querySelectorAll('#groupsTable tbody tr'));
        search.addEventListener('input', () => {
          const q = search.value.trim().toLowerCase();
          rows.forEach(r => r.style.display = r.innerText.toLowerCase().includes(q) ? '' : 'none');
        });
    </script>
</body>
</html>
