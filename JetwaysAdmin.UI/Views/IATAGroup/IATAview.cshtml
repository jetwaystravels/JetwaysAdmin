<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>IATA Groups – Jetways</title>
  <style>
    :root{
      --bg:#f7f8fb;--panel:#ffffff;--text:#0f172a;--muted:#6b7280;--line:#e5e7eb;--primary:#2563eb;--primary-700:#1d4ed8;--chip:#eef2ff;
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{margin:0;background:var(--bg);color:var(--text);font:14px/1.4 system-ui,-apple-system,Segoe UI,Roboto,Ubuntu,"Helvetica Neue",Arial,"Noto Sans",sans-serif}
    .container{max-width:1200px;margin:28px auto;padding:0 20px}
    .crumbs{display:flex;gap:.5rem;align-items:center;color:var(--muted);font-size:13px;margin-bottom:10px}
    .crumbs a{color:inherit;text-decoration:none}
    .page-title{display:flex;align-items:center;gap:10px;margin:0 0 14px}
    .back{display:inline-flex;align-items:center;justify-content:center;width:28px;height:28px;border-radius:7px;border:1px solid var(--line);background:#fff;cursor:pointer}
    .panel{background:var(--panel);border:1px solid var(--line);border-radius:12px;box-shadow:0 2px 8px rgba(15,23,42,.04)}

    /* toolbar */
    .toolbar{display:flex;gap:12px;justify-content:space-between;align-items:center;padding:12px 14px;border-bottom:1px solid var(--line)}
    .searchbox{position:relative;max-width:360px;flex:1}
    .searchbox input{width:100%;height:36px;padding:0 36px 0 36px;border-radius:9px;border:1px solid var(--line);background:#fafafa;outline:none}
    .searchbox svg{position:absolute;left:10px;top:50%;transform:translateY(-50%)}
    .add-btn{display:inline-flex;gap:8px;align-items:center;background:var(--primary);color:#fff;border:none;border-radius:9px;padding:8px 12px;cursor:pointer}
    .add-btn:hover{background:var(--primary-700)}

    /* table */
    table{width:100%;border-collapse:separate;border-spacing:0}
    thead th{font-weight:600;font-size:12px;color:var(--muted);text-align:left;padding:10px 14px;background:#fafafa;border-bottom:1px solid var(--line)}
    tbody td{padding:14px;border-bottom:1px solid var(--line);vertical-align:middle}
    tbody tr:hover{background:#fcfcff}
    .status-cell{width:110px}

    /* toggle switch */
    .switch{--w:38px;--h:22px;--p:3px;position:relative;display:inline-block;width:var(--w);height:var(--h)}
    .switch input{opacity:0;width:0;height:0}
    .slider{position:absolute;cursor:pointer;top:0;left:0;right:0;bottom:0;background:#d1d5db;border-radius:9999px;transition:all .25s}
    .slider:before{content:"";position:absolute;height:calc(var(--h) - var(--p)*2);width:calc(var(--h) - var(--p)*2);left:var(--p);top:var(--p);background:white;border-radius:50%;transition:all .25s;box-shadow:0 1px 3px rgba(0,0,0,.2)}
    .switch input:checked + .slider{background:#60a5fa}
    .switch input:checked + .slider:before{transform:translateX(calc(var(--w) - var(--h)))}

    /* footer */
    .footer{display:flex;justify-content:space-between;align-items:center;padding:10px 14px;color:var(--muted);font-size:12px}
    .pager{display:flex;align-items:center;gap:10px}
    .chip{display:inline-flex;align-items:center;gap:6px;padding:4px 8px;border:1px solid var(--line);border-radius:8px;background:#fff}
    .select{border:none;background:transparent;font:inherit;color:inherit;outline:none}
    .icon-btn{width:28px;height:28px;border:1px solid var(--line);background:#fff;border-radius:7px;display:inline-flex;align-items:center;justify-content:center;cursor:pointer}
    .muted{color:var(--muted)}
    .nowrap{white-space:nowrap}
    @@media (max-width:900px){
      .hide-md{display:none}
      .searchbox{max-width:100%}
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="crumbs"><a href="#">Settings</a><span>/</span><span>IATA Groups</span></div>
    <h2 class="page-title"><button class="back" aria-label="Back" title="Back" onclick="history.back()">
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6"/></svg>
    </button> IATA Groups</h2>

    <div class="panel">
      <div class="toolbar">
        <div class="searchbox">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"></circle><line x1="21" y1="21" x2="16.65" y2="16.65"></line></svg>
          <input id="search" placeholder="Search" aria-label="Search" />
        </div>
        <button class="add-btn" id="addNew">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="5" x2="12" y2="19"></line><line x1="5" y1="12" x2="19" y2="12"></line></svg>
          <span>+ Add New</span>
        </button>
      </div>

      <div class="table-wrap">
        <table id="groupsTable" aria-describedby="table-caption">
          <caption id="table-caption" class="sr-only">Manage IATA Group locations</caption>
          <thead>
            <tr>
              <th>Group Name</th>
              <th class="hide-md">Group Code</th>
              <th>City,State,Country</th>
              <th class="hide-md">Address</th>
              <th class="status-cell">Status</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>Jetways Delhi</td>
              <td class="hide-md">DEL</td>
              <td>India, Delhi, New Delhi</td>
              <td class="hide-md">CONNAUGHT PLACE , NEW DELHI , UGF …</td>
              <td class="status-cell">
                <label class="switch">
                  <input type="checkbox" checked aria-label="Jetways Delhi active" />
                  <span class="slider"></span>
                </label>
              </td>
            </tr>
            <tr>
              <td>Jetways Gurgaon</td>
              <td class="hide-md">—</td>
              <td>India, Haryana, Gurgaon</td>
              <td class="hide-md">—</td>
              <td class="status-cell">
                <label class="switch">
                  <input type="checkbox" checked aria-label="Jetways Gurgaon active" />
                  <span class="slider"></span>
                </label>
              </td>
            </tr>
            <tr>
              <td>Jetways Pune</td>
              <td class="hide-md">PNQ</td>
              <td>India, Delhi, New Delhi</td>
              <td class="hide-md">B. K. ROAD, CONNAUGHT PLACE, UGF …</td>
              <td class="status-cell">
                <label class="switch">
                  <input type="checkbox" checked aria-label="Jetways Pune active" />
                  <span class="slider"></span>
                </label>
              </td>
            </tr>
            <tr>
              <td>Jetways Mumbai</td>
              <td class="hide-md">—</td>
              <td>India, Maharashtra, Mumbai</td>
              <td class="hide-md">—</td>
              <td class="status-cell">
                <label class="switch">
                  <input type="checkbox" checked aria-label="Jetways Mumbai active" />
                  <span class="slider"></span>
                </label>
              </td>
            </tr>
          </tbody>
        </table>
      </div>

      <div class="footer">
        <div class="muted">1 – 4 of 4</div>
        <div class="pager">
          <span class="chip">Items per page:
            <select class="select" id="pageSize" aria-label="Items per page">
              <option selected>25</option>
              <option>50</option>
              <option>100</option>
            </select>
          </span>
          <button class="icon-btn" title="Previous page" aria-label="Previous page" disabled>
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6"/></svg>
          </button>
          <button class="icon-btn" title="Next page" aria-label="Next page" disabled>
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"/></svg>
          </button>
        </div>
      </div>
    </div>
  </div>

  <script>
    // Simple search filter
    const search = document.getElementById('search');
    const rows = Array.from(document.querySelectorAll('#groupsTable tbody tr'));
    search.addEventListener('input', () => {
      const q = search.value.trim().toLowerCase();
      rows.forEach(r => {
        const text = r.innerText.toLowerCase();
        r.style.display = text.includes(q) ? '' : 'none';
      });
    });

    // Demo: Add New button
    document.getElementById('addNew').addEventListener('click', () => {
      alert('Hook this up to your create-group flow.');
    });
  </script>
</body>
</html>
